build/
*.wasm

# Generated by templ. starter/ ships sources only: generating in place
# produces files importing the placeholder module path, which breaks
# `go build ./...` here. Copy the templates into your project first.
*_templ.go
