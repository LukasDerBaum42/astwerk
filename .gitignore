build/
*.wasm

# starter/ ships .templ sources only. Generating in place produces files that
# import the placeholder module path, which breaks `go build ./...` here.
# The site under site/ does commit its generated files.
starter/**/*_templ.go
